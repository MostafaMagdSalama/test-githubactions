on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:20
    steps:
      - runs: node --version
      - runs: npm --version
      - uses: actions/checkout@v3
      - runs: npm install
      - runs: npm run test

